import 'package:flutter/material.dart';

class AddReelPage extends StatelessWidget {
  const AddReelPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}